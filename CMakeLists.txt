cmake_minimum_required(VERSION 3.10)
project(InaccuratePi)
add_subdirectory(qc)
add_executable(inaccurate-pi inaccurate-pi.c needle.c)
target_link_libraries(inaccurate-pi PUBLIC qc)
if(NOT WIN32)
	target_link_libraries(inaccurate-pi PUBLIC m)
endif()
